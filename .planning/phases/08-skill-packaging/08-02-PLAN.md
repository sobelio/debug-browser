---
phase: 08-skill-packaging
plan: 02
type: execute
---

<objective>
Update skill documentation for standalone installation and verify end-to-end.

Purpose: The skill/README.md currently shows manual cargo build + symlink steps. Update to reflect the install script and new discovery mechanism.
Output: Updated docs, verified standalone installation.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-skill-packaging/08-01-SUMMARY.md

# Key files:
@skill/README.md
@scripts/install.sh

**Constraining decisions:**
- Phase 5: Skill structure (SKILL.md + references/ + README.md)
- Plan 08-01: Install script with --prefix layout, DEBUG_BROWSER_HOME env var
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update skill/README.md for standalone installation</name>
  <files>skill/README.md</files>
  <action>
Rewrite skill/README.md to reflect the new install methods:

**Prerequisites** section: Keep Rust toolchain + Node.js 18+ requirements.

**Quick Install** section (new, primary method):
```bash
git clone <repo-url>
cd debug-browser
./scripts/install.sh
```
Mention --prefix for custom location.

**Development Install** section (for contributors):
- cargo build --release
- cd daemon && npm install && npm run build
- Binary auto-discovers daemon via cargo target/ layout

**Claude Code Skill Install** section:
- Symlink: `ln -s $(pwd)/skill ~/.claude/skills/debug-browser`
- Or copy
- Note: The skill tells Claude about the CLI commands; the CLI binary must be on PATH

**Environment Variables** section (new):
- `DEBUG_BROWSER_HOME` — root of the install (auto-discovered if binary is in standard layout)
- `DEBUG_BROWSER_DAEMON_PATH` — explicit path to daemon.js (overrides all other discovery)

**Uninstall** section: `./scripts/uninstall.sh --prefix ~/.local`

Keep the Skill Contents tree at the bottom. Do NOT change SKILL.md or the references/ files.
  </action>
  <verify>cat skill/README.md (should show new sections, no broken links)</verify>
  <done>README has Quick Install, Development Install, Skill Install, Environment Variables, and Uninstall sections</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end standalone verification</name>
  <files>scripts/install.sh, src/connection.rs</files>
  <action>
Run the full install-and-verify cycle:

1. Run `scripts/install.sh --prefix /tmp/debug-browser-e2e`
2. Verify binary exists and runs: `/tmp/debug-browser-e2e/bin/debug-browser --help`
3. Verify daemon files exist: `ls /tmp/debug-browser-e2e/share/debug-browser/daemon/dist/daemon.js`
4. Verify daemon scripts exist: `ls /tmp/debug-browser-e2e/share/debug-browser/daemon/dist/scripts/`
5. Verify node_modules exist: `ls /tmp/debug-browser-e2e/share/debug-browser/daemon/node_modules/playwright-core/`
6. Clean up: `rm -rf /tmp/debug-browser-e2e`

If any step fails, fix the issue in the relevant file (install script or connection.rs).

Phase 8 complete after this task.
  </action>
  <verify>All verification commands above pass without errors</verify>
  <done>Standalone install works end-to-end: binary finds daemon from share/ layout, all required files present</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] skill/README.md updated with new install methods
- [ ] End-to-end install + verify cycle passes
- [ ] Clean up test install directory
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- README accurately reflects install process
- Phase 8 complete
</success_criteria>

<output>
After completion, create `.planning/phases/08-skill-packaging/08-02-SUMMARY.md`
</output>
