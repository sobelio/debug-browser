---
phase: 09-nix-flake
plan: 02
type: execute
---

<objective>
Polish the flake with a dev shell and update documentation with Nix install instructions.

Purpose: Make the flake useful for both end-users (install) and contributors (develop).
Output: Complete devShell, updated README with Nix install method.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-nix-flake/09-01-SUMMARY.md
@.planning/phases/09-nix-flake/09-RESEARCH.md

# Key files:
@flake.nix
@skill/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Nix install section to skill/README.md</name>
  <files>skill/README.md</files>
  <action>
Add a "Nix Install" section to skill/README.md, positioned after "Quick Install" and before "Development Install".

Content:
- `nix profile install github:sobelio/debug-browser` (once repo is public)
- `nix build` from local clone
- Note: On Linux, Playwright browser is included automatically. On macOS, run `npx playwright install chromium` or ensure Google Chrome is installed.
- `nix develop` for development shell with Rust + Node.js toolchains

Also add a "Flake Input" subsection showing how to use as a dependency:
```nix
{
  inputs.debug-browser.url = "github:sobelio/debug-browser";
}
```

Keep existing sections intact. Just add the new Nix section.
  </action>
  <verify>cat skill/README.md | grep -A5 "Nix Install" (shows new section)</verify>
  <done>README has Nix Install section with profile install, local build, and flake input instructions</done>
</task>

<task type="auto">
  <name>Task 2: Verify nix develop shell works</name>
  <files>flake.nix</files>
  <action>
Verify the development shell works:

1. Run `nix develop --command bash -c "cargo --version && node --version && npm --version"` — all three tools should be available
2. Run `nix develop --command bash -c "cd daemon && npm run build"` — daemon should build in dev shell
3. Run `nix develop --command bash -c "cargo build"` — Rust should build in dev shell

If the devShell is missing or incomplete in flake.nix, add it:
```nix
devShells.default = craneLib.devShell {
  packages = with pkgs; [ nodejs ];
};
```

craneLib.devShell automatically includes Rust toolchain. We just need to add nodejs.

If devShell already exists from 09-01, just verify it works.

Phase 9 complete after this task.
  </action>
  <verify>nix develop --command bash -c "cargo --version && node --version" succeeds</verify>
  <done>Dev shell provides Rust + Node.js toolchains, daemon and CLI build within it</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] skill/README.md has Nix Install section
- [ ] `nix develop` provides cargo, node, npm
- [ ] Both daemon and CLI build within dev shell
</verification>

<success_criteria>

- All tasks completed
- Documentation updated with Nix install methods
- Dev shell works for contributors
- Phase 9 complete
</success_criteria>

<output>
After completion, create `.planning/phases/09-nix-flake/09-02-SUMMARY.md`
</output>
