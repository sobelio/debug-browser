---
phase: 06-automated-testing
plan: 01
type: execute
---

<objective>
Create a minimal React test fixture app with deterministic component tree, hooks, and console output for E2E testing.

Purpose: Provide a stable, predictable React app that tests can assert against. Every component name, hook value, and console message is known ahead of time.
Output: tests/fixture/ directory with a Vite+React app ready to serve on localhost:5188.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# CLI command reference (tests must match these commands):
@skill/references/commands.md

# Source for exact CLI flags and subcommands:
@src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Vite+React fixture app with known component tree</name>
  <files>tests/fixture/package.json, tests/fixture/vite.config.ts, tests/fixture/index.html, tests/fixture/src/main.tsx, tests/fixture/src/App.tsx, tests/fixture/src/components/TodoList.tsx, tests/fixture/src/components/Counter.tsx, tests/fixture/tsconfig.json</files>
  <action>
Create a minimal Vite+React app in tests/fixture/ that serves a predictable component tree.

**Package setup:**
- `npm create vite@latest` equivalent: package.json with react, react-dom, vite, @vitejs/plugin-react, typescript
- vite.config.ts: serve on port 5188 (avoid conflicts with common ports)

**Component tree (these exact names and structure matter for test assertions):**

```
App
├── Counter (useState with count starting at 0, useEffect logging "Counter mounted", useCallback for increment)
├── TodoList (useState with items=["Buy milk","Walk dog"], useMemo for item count, useRef for input element)
│   ├── TodoItem key="Buy milk" (props: text, onDelete)
│   └── TodoItem key="Walk dog" (props: text, onDelete)
└── ConsoleDemo (useEffect that logs "Hello from ConsoleDemo" and console.error("Test error message") on mount)
```

**Key requirements:**
- Counter: `useState(0)` — tests can click increment button and verify state changes
- TodoList: `useState(["Buy milk", "Walk dog"])` — tests can verify component filtering and hook values
- TodoList has an `<input id="todo-input">` and `<button id="add-todo">` — tests can use click/type commands
- ConsoleDemo: fires `console.log("Hello from ConsoleDemo")` and `console.error("Test error message")` on mount — tests can verify console capture
- All components are function components (not class) so hooks inspection works
- Counter has a `<button id="increment">` element for click testing
- App renders a `<h1>Debug Browser Test Fixture</h1>` for eval testing

Do NOT add any test framework or test files — this is just the fixture app.
  </action>
  <verify>cd tests/fixture && npm install && npm run build succeeds without errors</verify>
  <done>tests/fixture/ contains a buildable Vite+React app with App, Counter, TodoList, TodoItem, and ConsoleDemo components matching the specified tree structure</done>
</task>

<task type="auto">
  <name>Task 2: Add npm scripts and verify fixture serves correctly</name>
  <files>tests/fixture/package.json</files>
  <action>
Ensure the fixture app has these scripts in package.json:
- `"dev": "vite --port 5188"` — for development
- `"build": "tsc && vite build"` — for production build
- `"preview": "vite preview --port 5188"` — serve production build (tests will use this)

Verify the app builds and the preview server starts. Kill the preview server after verification.

Also add a `"serve-test": "vite --port 5188 --host 127.0.0.1"` script for test runner usage (foreground, easy to kill).
  </action>
  <verify>cd tests/fixture && npm run build && timeout 5 npm run preview || true (build succeeds, preview starts)</verify>
  <done>Fixture app builds without errors and can serve on port 5188</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd tests/fixture && npm install` succeeds
- [ ] `cd tests/fixture && npm run build` succeeds
- [ ] Component tree matches specification: App > Counter, TodoList > TodoItem, ConsoleDemo
- [ ] Counter has useState(0) and increment button with id="increment"
- [ ] TodoList has useState with 2 items and input with id="todo-input"
- [ ] ConsoleDemo logs known messages on mount
</verification>

<success_criteria>

- All tasks completed
- Fixture app builds without errors
- Component tree is deterministic and matches specification
- Known hook values, console messages, and DOM elements for test assertions
</success_criteria>

<output>
After completion, create `.planning/phases/06-automated-testing/06-01-SUMMARY.md`:

# Phase 6 Plan 1: Test Fixture React App Summary

**[Substantive one-liner]**

## Accomplishments
## Files Created/Modified
## Decisions Made
## Issues Encountered
## Next Step

Ready for 06-02-PLAN.md (test harness and core command tests).
</output>
