---
phase: 06-automated-testing
plan: 03
type: execute
---

<objective>
Write E2E tests for interaction commands (click, type, eval), console capture (logs, errors, clear), and JSON output format.

Purpose: Complete test coverage of all 11 CLI commands, ensuring the full interaction + inspection loop works.
Output: Additional test functions in tests/run-tests.sh covering all remaining commands.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-automated-testing/06-01-SUMMARY.md
@.planning/phases/06-automated-testing/06-02-SUMMARY.md

# Test harness (from Plan 2):
@tests/run-tests.sh

# Fixture components (for knowing what to click/type/assert):
@tests/fixture/src/App.tsx
@tests/fixture/src/components/Counter.tsx
@tests/fixture/src/components/TodoList.tsx

# CLI reference:
@skill/references/commands.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write interaction command tests (click, type, eval)</name>
  <files>tests/run-tests.sh</files>
  <action>
Add test functions to run-tests.sh for interaction commands. These tests verify the interact → inspect loop.

**Test: click (Counter increment)**
```bash
# Get initial Counter state
output=$($CLI hooks Counter)
assert_contains "$output" "0" "Counter starts at 0"

# Click increment button
click_output=$($CLI click "#increment")
assert_contains "$click_output" "Clicked" "click returns confirmation"

# Verify state changed
sleep 0.5  # brief wait for React re-render
output=$($CLI hooks Counter)
assert_contains "$output" "1" "Counter incremented to 1 after click"
```

**Test: type (TodoList input)**
```bash
type_output=$($CLI type "#todo-input" "New todo item")
assert_contains "$type_output" "Typed" "type returns confirmation"
```

**Test: eval**
```bash
output=$($CLI eval "document.title")
assert_contains "$output" "Debug Browser" "eval returns page title"

# Eval with numeric result
output=$($CLI eval "1 + 1")
assert_contains "$output" "2" "eval returns computed value"

# Eval checking DOM
output=$($CLI eval "document.querySelector('h1').textContent")
assert_contains "$output" "Debug Browser Test Fixture" "eval reads h1 text"
```

**Test: eval JSON format**
```bash
json=$($CLI --format json eval "document.title")
assert_contains "$json" '"success":true' "eval JSON success"
```

Group as: `test_click()`, `test_type()`, `test_eval()`.
  </action>
  <verify>bash tests/run-tests.sh (interaction tests pass)</verify>
  <done>click, type, and eval commands verified with state change assertions</done>
</task>

<task type="auto">
  <name>Task 2: Write console capture and JSON output tests</name>
  <files>tests/run-tests.sh</files>
  <action>
Add test functions for console capture commands and comprehensive JSON output testing.

**Test: console errors**
```bash
# ConsoleDemo fires console.error on mount, should be in inbox
output=$($CLI console errors)
assert_contains "$output" "Test error message" "console errors captures error from ConsoleDemo"
```

**Test: console logs**
```bash
output=$($CLI console logs)
assert_contains "$output" "Hello from ConsoleDemo" "console logs captures log from ConsoleDemo"
```

**Test: console clear**
```bash
$CLI console clear > /dev/null
output=$($CLI console errors)
# After clear, should be empty (no "Test error message")
TESTS=$((TESTS + 1))
if ! echo "$output" | grep -q "Test error message"; then
  PASSED=$((PASSED + 1))
  echo "  ✓ console clear removes errors"
else
  FAILED=$((FAILED + 1))
  echo "  ✗ console clear removes errors"
fi
```

**Test: console clear JSON format**
```bash
json=$($CLI --format json console clear)
assert_contains "$json" '"success":true' "console clear JSON success"
```

**Test: components JSON format**
```bash
json=$($CLI --format json components --depth 2)
assert_contains "$json" '"success":true' "components JSON success"
assert_contains "$json" '"roots"' "components JSON has roots"
assert_contains "$json" '"componentCount"' "components JSON has componentCount"
```

**Test: --session flag (basic)**
```bash
# Just verify a named session works (starts a new daemon)
output=$($CLI --session test-session navigate http://127.0.0.1:5188)
assert_contains "$output" "Navigated" "named session navigate works"
$CLI --session test-session close > /dev/null 2>&1
```

Add an `assert_not_contains()` helper for the console clear test.

Group as: `test_console()`, `test_json_output()`, `test_session()`.

**Finally:** Ensure all test groups are called from main flow in correct order:
1. test_navigate
2. test_react_detect
3. test_components
4. test_hooks
5. test_click
6. test_type
7. test_eval
8. test_console
9. test_json_output
10. test_session
11. test_close (last — shuts down daemon)

Print final summary with total pass/fail count and exit code.
  </action>
  <verify>cd /Users/whn/code/debug-browser && bash tests/run-tests.sh (all tests pass, clean exit)</verify>
  <done>All 11 commands tested, console capture verified, JSON output verified, clean pass/fail summary printed</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash tests/run-tests.sh` runs all tests to completion
- [ ] All 11 commands have at least one test assertion
- [ ] Console capture tests verify accumulation and clearing
- [ ] JSON output tests verify structured response format
- [ ] No orphan processes after test run
- [ ] Phase 6 complete — all automated testing in place
</verification>

<success_criteria>

- All tasks completed
- Full command coverage: navigate, react detect, components, hooks, click, type, eval, console logs, console errors, console clear, close
- JSON format tested for key commands
- Tests pass reliably on clean run
- Phase 6: Automated Testing complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-automated-testing/06-03-SUMMARY.md`:

# Phase 6 Plan 3: Interaction & Output Tests Summary

**[Substantive one-liner]**

## Accomplishments
## Files Created/Modified
## Decisions Made
## Issues Encountered
## Next Step

Phase 6 complete. All phases done — project is 100% complete.
</output>
