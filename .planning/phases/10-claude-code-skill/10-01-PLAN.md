---
phase: 10-repo-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - LICENSE
  - Cargo.toml
  - .gitignore
autonomous: true
requirements: []

must_haves:
  truths:
    - "README.md explains what debug-browser is, how to install it, and how to use it"
    - "LICENSE file contains MIT license text with correct copyright"
    - "Cargo.toml version is 1.1.0"
    - ".gitignore excludes build artifacts and transient files"
  artifacts:
    - path: "README.md"
      provides: "Project README with install/usage instructions"
      min_lines: 80
    - path: "LICENSE"
      provides: "MIT license file"
      contains: "MIT License"
  key_links:
    - from: "README.md"
      to: "skill/README.md"
      via: "reference link for skill-specific docs"
      pattern: "skill/README"
---

<objective>
Create a polished README.md, MIT LICENSE file, bump version to 1.1.0, and clean up .gitignore for public release.

Purpose: Prepare the repository for public visibility at sobelio/debug-browser.
Output: README.md, LICENSE, updated Cargo.toml version, clean .gitignore
</objective>

<execution_context>
@/Users/whn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/whn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skill/README.md
@Cargo.toml
@CLAUDE.md
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md and LICENSE</name>
  <files>README.md, LICENSE</files>
  <action>
Create README.md at repository root with these sections (in order):

1. **Title and badges** — `# debug-browser` with a one-line description: "A Rust CLI + Claude Code skill for debugging React applications via headless browser with rich introspection capabilities."

2. **Features** — Bullet list of key capabilities:
   - React component tree inspection (props, state, hooks)
   - Console log / error / warning capture with inbox model
   - DOM interaction (click, type, screenshot)
   - Navigation and page management
   - Claude Code skill for AI-assisted React debugging
   - Nix flake for reproducible builds

3. **Quick Start** — Minimal example showing clone, build, and basic usage:
   ```
   git clone https://github.com/sobelio/debug-browser
   cd debug-browser
   ./scripts/install.sh
   debug-browser navigate http://localhost:3000
   debug-browser react tree
   ```

4. **Installation** — Three methods:
   - **Install script** (recommended): `./scripts/install.sh` with PATH note
   - **Nix**: `nix profile install github:sobelio/debug-browser` with platform notes (macOS needs Chrome)
   - **From source**: `cargo build --release` + `cd daemon && npm install && npm run build`

5. **Usage** — Key commands with brief examples:
   - `debug-browser navigate <url>` — open a page
   - `debug-browser react tree` — show component tree
   - `debug-browser react inspect <id>` — inspect component props/state/hooks
   - `debug-browser console` — show captured console messages
   - `debug-browser click <selector>` — interact with DOM
   - `debug-browser screenshot` — capture screenshot
   - Point to skill/README.md for full command reference

6. **Claude Code Skill** — Brief section explaining the skill, link to skill/README.md for details, show the symlink install:
   ```
   ln -s $(pwd)/skill ~/.claude/skills/debug-browser
   ```

7. **Architecture** — Brief description: Rust CLI thin client communicates over Unix socket with Node.js daemon running Playwright. Diagram not needed, just 2-3 sentences.

8. **Development** — `nix develop` or manual setup, running tests with `cargo test`

9. **License** — "MIT" with link to LICENSE file

Create LICENSE file with standard MIT license text. Copyright line: "Copyright (c) 2026 Sobelio". Year 2026, matching the project timeline.

Do NOT include emojis anywhere in README or LICENSE.
  </action>
  <verify>
    <automated>test -f README.md && test -f LICENSE && grep -q "MIT" LICENSE && grep -q "sobelio/debug-browser" README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>README.md exists with install/usage/architecture sections. LICENSE exists with MIT text and Sobelio copyright.</done>
</task>

<task type="auto">
  <name>Task 2: Bump version and clean gitignore</name>
  <files>Cargo.toml, .gitignore</files>
  <action>
Update Cargo.toml:
- Change `version = "0.1.0"` to `version = "1.1.0"`
- Add `repository = "https://github.com/sobelio/debug-browser"` to [package] section
- Add `readme = "README.md"` to [package] section
- Keep all other fields as-is

Update .gitignore to ensure these are excluded (add any missing):
- `/target` (already present)
- `daemon/dist/` (already present)
- `daemon/node_modules/` (already present)
- `.DS_Store` (already present)
- `*.swp`, `*.swo` (already present)
- `result` (already present — Nix build output)
- `/try-debug-browser/` (test directory, should not be in public repo)
- `.planning/` (planning docs should not be in public repo)

Run `cargo check` to verify Cargo.toml is valid after edits.
  </action>
  <verify>
    <automated>grep -q 'version = "1.1.0"' Cargo.toml && grep -q 'repository' Cargo.toml && grep -q '.planning' .gitignore && cargo check 2>&1 | tail -1</automated>
  </verify>
  <done>Cargo.toml has version 1.1.0 with repository URL. .gitignore excludes planning docs and test directories.</done>
</task>

</tasks>

<verification>
- README.md is comprehensive and has install + usage + architecture sections
- LICENSE contains MIT license with Sobelio copyright
- Cargo.toml version is 1.1.0 with repository field
- .gitignore excludes .planning/ and try-debug-browser/
- `cargo check` passes
</verification>

<success_criteria>
Repository is ready for public visibility: README explains the project, LICENSE is present, version is correct, sensitive/dev-only directories are gitignored.
</success_criteria>

<output>
After completion, create `.planning/phases/10-claude-code-skill/10-01-SUMMARY.md`
</output>
